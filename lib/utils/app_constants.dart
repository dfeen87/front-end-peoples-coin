// Constants for responsive design breakpoints
class AppBreakpoints {
  static const double desktop = 1000.0; // Wider screens
  static const double tablet = 600.0;   // Medium screens
  static const double mobile = 0.0;     // Small screens (default for anything below tablet)
}
