name: brightacts_frontend_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.1
  firebase_auth: ^5.0.0 # Keep this for Firebase Auth (which has web support)
  provider: ^6.1.2
  tflite_flutter: ^0.10.0 # Keep for Ambient AI
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  url_launcher: ^6.2.2
  shimmer: ^3.0.0
  animated_digit: ^3.2.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
